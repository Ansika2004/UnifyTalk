import { AccessibleChat } from '@/components/AccessibleChat'

export default function ChatPage() {
  return (
    <main id="main-content" className="pb-20 h-screen flex flex-col">
      <AccessibleChat />
    </main>
  )
}
